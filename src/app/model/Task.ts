export class Task{
    /*
    *!=  ! is a non-null assertion operator 
    *that informs TypeScript that the property will not be null or undefined at runtime.
    */
    id !: number;
    task !:String;
}
